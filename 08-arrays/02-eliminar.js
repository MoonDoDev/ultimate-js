const letras = ['a', 'b', 'c', 'd'];

// Eliminamos un elemento al final del array
// const final = letras.pop();
// console.log(final, letras);

// Eliminamos un elemento al inicio del array
// const comienzo = letras.shift();
// console.log(comienzo, letras);

// Eliminamos uno o varios elementos a partir de una posición específica de un array
letras.splice( 1, 2 );
console.log( letras );
